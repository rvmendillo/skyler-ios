name: Build Software Life IPA

on:
  push:
    paths:
      - 'SoftwareLife/**'
      - '.github/workflows/software-life-ios.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Tuist
        run: curl -Ls https://install.tuist.io | bash

      - name: Generate Xcode project
        working-directory: SoftwareLife
        run: tuist generate --no-open

      - name: Build unsigned app
        working-directory: SoftwareLife
        run: |
          set -o pipefail
          xcodebuild \
            -project SoftwareLife.xcodeproj \
            -scheme SoftwareLife \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY='' \
            build

      - name: Package IPA
        working-directory: SoftwareLife
        run: |
          APP_PATH=$(find build/Build/Products/Release-iphoneos -maxdepth 1 -name '*.app' | head -1)
          test -n "$APP_PATH"
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          ditto -c -k --sequesterRsrc --keepParent Payload SoftwareLife-unsigned.ipa

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: SoftwareLife-unsigned-ipa
          path: SoftwareLife/SoftwareLife-unsigned.ipa
          if-no-files-found: error
